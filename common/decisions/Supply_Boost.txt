# カテゴリファイルで設定した識別名（今回はSB）
SB = {

    # 各ディシジョンの記述領域

    emergency_construction = {
        icon = generic_industry
        priority = 10
        available = {
            has_tech = tech_logistics_company
        }
        visible = {
            always = yes
        }
        allowed = {
            always = yes
        }
        fire_only_once = yes
        ai_will_do = {
            factor = 1
        }
        cost = 200
        complete_effect = {
            set_country_flag = logistics_modernized_flag
            log = "Logistics Modernization Flag set for [?root.GetTag]"
        }
    }

    # ディシジョン識別名（今回はemergency_airlift_request | わかりやすい方がいい！）
    emergency_airlift_request = {

        # ディシジョンのアイコン
        icon = generic_industry

        # ディシジョンの表示優先度（他のディシジョンとのバランスによって調整したり！）
        priority = 10

        # ディシジョンの実行条件（例：特定の技術が研究されていること）
        available = {

            # 今回は研究されていることを条件に！（空軍の内容に合うように調整）
            has_tech = tech_logistics_company2      # レベル2輸送機
            has_air_experience > 10                 # 空軍経験値10以上持ってるとき

            # 装備を在庫数を条件に（今回は輸送なので輸送機を100機以上）
            has_equipment = { transport_plane_equipment > 99 }
        }

        # ディシジョンの表示・非表示条件（例：個別の国家でのみ閲覧できるようにする）
        visible = {

            # 今回は別に前提ディシジョンを用意してそれが取得が出来た場合に表示する
            # つけなくてもOK！詳細はGitHubをご覧ください！
            has_country_flag = logistics_modernized_flag
        }

        # 表示権限（国家に対しての表示条件指定｜経験値の指定をしても押せる。上のは押せなくなる)
        allowed = {
            # 全国家に常に表示する
            always = yes
        }

        # 一度きりの実行にするか？（yesの場合、実行後は再表示☓）
        fire_only_once = no

        # AIのディシジョン優先度
        ai_will_do = {
            factor = 1
        }

        # ディシジョン有効化に必要な政治力（ゲームバランスを壊さない程度に！）
        cost = 200

        # 完了してから再度選べるようになるまでのクールダウン期間（今回は14日｜連打して爆速にしたい思いを乗せて～！）
        days_re_enable = 14

        # ディシジョン効果日数（今回は180日間効果が続く｜クールダウンを含めて1年で2階程度は使える構成に！）
        days_remove = 180

        # ステータス補正（例：建築速度 / 研究速度上昇）
        modifier = {
            # --- バフ --- （ホントは輸送機を強化したかった...）
            supply_consumption_factor = -0.1   # 物資消費を15%抑制
            out_of_supply_factor = -0.15       # 補給切れペナルティを20%軽減
          
            # --- デバフ ---（輸送機が酷使されてる感じを演出）
            air_accidents_factor = 0.25        # 航空事故率20%増加
            air_range_factor = -0.05           # 航空機の航続距離を-10%低下
        }

        # ディシジョン終了時に発動される効果（例：政治力付与）
        remove_effect = {
        }

        # 国家全体に発動される効果（例：国民精神の付与）
        complete_effect = {
            # 実行した瞬間に、空軍経験値を少し消費する
            air_experience = -10
        }
    }

    authorize_overloading = {
        icon = generic_industry
        priority = 10
        available = {
            has_tech = tech_logistics_company3
            has_tech = landing_craft
        }
        visible = {
            has_country_flag = logistics_modernized_flag
        }
        allowed = {
            always = yes
        }
        fire_only_once = no
        ai_will_do = {
            factor = 1
        }
        cost = 200
        days_re_enable = 14
        days_remove = 180
        modifier = {
            no_supply_grace = 120
            org_loss_when_moving = 0.05
            experience_gain_navy_factor = -0.05
        }
        remove_effect = {
        }
        complete_effect = {
        }
    }

    deploy_railway_regiments = {
        icon = generic_industry
        priority = 10
        available = {
            has_tech = tech_logistics_company
            has_tech = wartime_train
            has_tech = construction3
        }
        visible = {
            has_country_flag = logistics_modernized_flag
        }
        allowed = {
            always = yes
        }
        fire_only_once = no
        ai_will_do = {
            factor = 1
        }
        cost = 200
        days_re_enable = 14
        days_remove = 180
        modifier = {
            production_speed_buildings_factor = 0.15
        }
        remove_effect = {
        }
        complete_effect = {
        }
    }

    track_gauge_conversion = {
        icon = generic_industry
        priority = 10
        available = {
            has_tech = tech_logistics_company
            has_tech = construction2
            has_war = yes
        }
        visible = {
            has_country_flag = logistics_modernized_flag
        }
        allowed = {
            always = yes
        }
        fire_only_once = no
        ai_will_do = {
            factor = 1
        }
        cost = 200
        days_re_enable = 14
        days_remove = 180
        modifier = {
            production_speed_buildings_factor = 0.30
            experience_gain_army_factor = -0.05
        }
        remove_effect = {
        }
        complete_effect = {
        }
    }

    deploy_railway_regiments = {
        # ディシジョンのアイコン
        icon = generic_industry

        #ディシジョンの表示優先度
        priority = 10

        # ディシジョンの実行条件（例：特定の技術が研究されていること）
        available = {
            # 特定の技術が研究されていることを条件にする
            has_tech = tech_logistics_company
            has_tech = wartime_train
            has_tech = construction3
        }

        # ディシジョンの表示条件（例：個別の国家でのみ閲覧できるようにする）
        visible = {
            has_country_flag = logistics_modernized_flag
        }

        #実行権限と表示権限 
        allowed = {
            #全国家に常に表示する
            always = yes
        }

        # 一度きりの実行にしますか？（yesの場合、実行後は再度表示されなくなります）
        fire_only_once = no

        # AIのディシジョン優先度
        ai_will_do = {
            factor = 1
        }

        # ディシジョン有効化に必要な政治力
        cost = 200

        # 完了してから再度選べるようになるまでのクールダウン期間（例：365日間）
        days_re_enable = 14

        # ディシジョン効果日数（例：180日間効果が続く）
        days_remove = 180

        # ステータス補正（例：建築速度上昇）
        modifier = {
            production_speed_buildings_factor = 0.15
        }

        # ディシジョン終了時に発動される効果（例：政治力付与）
        remove_effect = {
            
        }

        # 国家全体に対して発動される効果（例：研究速度上昇）
        complete_effect = {
            
        }
    }

    track_gauge_conversion = {
        # ディシジョンのアイコン
        icon = generic_industry

        #ディシジョンの表示優先度
        priority = 10

        # ディシジョンの実行条件（例：特定の技術が研究されていること）
        available = {
            # 特定の技術が研究されていることを条件にする
            has_tech = tech_logistics_company
            has_tech = construction2
            has_war = yes
        }

        # ディシジョンの表示条件（例：個別の国家でのみ閲覧できるようにする）
        visible = {
            has_country_flag = logistics_modernized_flag
        }

        #実行権限と表示権限 
        allowed = {
            #全国家に常に表示する
            always = yes
        }

        # 一度きりの実行にしますか？（yesの場合、実行後は再度表示されなくなります）
        fire_only_once = no

        # AIのディシジョン優先度
        ai_will_do = {
            factor = 1
        }

        # ディシジョン有効化に必要な政治力
        cost = 200

        # 完了してから再度選べるようになるまでのクールダウン期間（例：365日間）
        days_re_enable = 14

        # ディシジョン効果日数（例：180日間効果が続く）
        days_remove = 180

        # ステータス補正（例：建築速度上昇）
        modifier = {
            production_speed_buildings_factor = 0.30
            experience_gain_army_factor = -0.05 # 陸軍経験値獲得量を10%増加
        }

        # ディシジョン終了時に発動される効果（例：政治力付与）
        remove_effect = {
            
        }

        # 国家全体に対して発動される効果（例：研究速度上昇）
        complete_effect = {
            
        }
    }
}